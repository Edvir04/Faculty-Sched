import { Badge } from '@/components/ui/badge';

export default function DefaultCurriculumBadge() {
    return (
        <Badge variant="secondary" className="ml-2 font-serif text-xs font-normal">
            Default
        </Badge>
    );
}
